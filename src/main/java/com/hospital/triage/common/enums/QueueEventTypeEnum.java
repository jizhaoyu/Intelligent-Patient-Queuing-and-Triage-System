package com.hospital.triage.common.enums;

public enum QueueEventTypeEnum {
    ENQUEUE,
    CALL_NEXT,
    RECALL,
    MISSED,
    COMPLETE,
    CANCEL,
    MANUAL_ADJUST,
    GRANT_PRIORITY_REVISIT
}
